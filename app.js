//const fullName= "Nduka Asoh Obewu";
const bioData = {
    fullName: `Nduka Asoh Obewu`,
    Sex: "Male",
    Married: true,
    Age: 36,
    Blood: "O+",
    Genotype: "As",
    Occupation: "Technology Support",
  };
  console.log(bioData);

  // Datatypes of the Objects

  console.log(
    typeof bioData.fullName,
    typeof bioData.Sex,
    typeof bioData.Married,
    typeof bioData.Age,
    typeof bioData.Blood,
    typeof bioData.Genotype,
    typeof bioData.Occupation
  );
  